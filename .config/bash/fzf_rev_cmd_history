#!/usr/bin/env bash

selection=$(history \
  | sed 's/^.\{7\}//' \
  | uniq \
  | rg "$READLINE_LINE" \
  | fzf --tac --no-sort --query "$READLINE_LINE")

test -n "$selection" \
  && READLINE_LINE="$selection" \
  && READLINE_POINT=${#READLINE_LINE}
